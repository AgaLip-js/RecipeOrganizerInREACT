import React from "react";
// import { useSelector } from "react-redux";

const RecipeCategoryView = () => {

  // const { myRecipies } = useSelector((store) => ({
  //   myRecipies: store.myRecipies
  // }));

  return (
    <div>

    </div>
  );
};

export default RecipeCategoryView;
